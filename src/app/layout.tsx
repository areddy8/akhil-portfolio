import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Akhil Reddy — Analytics Engineering + Data Science",
  description:
    "Metrics, warehouse models, A/B experimentation, and AI analytics tools. Senior Data Scientist building decision systems for lending and pricing.",
  openGraph: {
    title: "Akhil Reddy — Analytics Engineering + Data Science",
    description:
      "Metrics, warehouse models, A/B experimentation, and AI analytics tools.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} min-h-screen bg-background text-foreground antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
